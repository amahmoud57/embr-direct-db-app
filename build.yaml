platform: nodejs
platformVersion: "20"

run:
  port: 3000
  startCommand: node server.js

database:
  enabled: true
  engine: postgres
  # Mode (clone/direct/internal) is now set per environment via Portal/CLI
  # Connection URL is stored securely in Portal, not in build.yaml
  # connection: production  # Optional - only needed if multiple external DBs exist
